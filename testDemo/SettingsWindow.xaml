<Window x:Class="testDemo.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置目标 IP 和端口" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="目标 IP：" 
                   VerticalAlignment="Center" />
        <TextBox x:Name="IpTextBox" 
                 Grid.Row="0" 
                 Margin="100,0,0,0" 
                 Width="200" />

        <TextBlock Text="目标端口：" 
                   Grid.Row="1" 
                   VerticalAlignment="Center" />
        <TextBox x:Name="PortTextBox" 
                 Grid.Row="1" 
                 Margin="100,0,0,0" 
                 Width="200" />

        <TextBlock Text="站位信息：" 
           Grid.Row="2" 
           VerticalAlignment="Center" />
        <TextBox x:Name="EquipmentTextBox" 
         Grid.Row="2" 
         Margin="100,0,0,0" 
         Width="200" />

        <Button Content="保存" 
                Grid.Row="3" 
                Margin="0,10,0,0" 
                HorizontalAlignment="Right" 
                Width="80"
                Click="SaveButton_Click" />
    </Grid>
</Window>

